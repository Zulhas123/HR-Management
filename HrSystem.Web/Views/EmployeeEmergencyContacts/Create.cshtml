@model HrSystem.Web.ViewModels.EmployeeEmergencyContactFormVm

@{
    ViewData["Title"] = "Add Emergency Contact";
}

<h1 class="h3">Add Emergency Contact</h1>

<form method="post" class="mt-3">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="EmployeeId" />
    <partial name="_EmergencyContactForm" model="Model" />
    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" asp-route-employeeId="@Model.EmployeeId" class="btn btn-secondary">Cancel</a>
</form>

