@model IReadOnlyList<HrSystem.Domain.Entities.EmployeeTransfer>

@{
    var employee = (HrSystem.Domain.Entities.Employee)ViewData["Employee"]!;
    ViewData["Title"] = "Transfers";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="h3 mb-0">Transfers</h1>
        <div class="text-muted">@employee.EmployeeCode - @employee.FirstName @employee.LastName</div>
    </div>
    <a class="btn btn-primary" asp-action="Create" asp-route-employeeId="@employee.Id">Add</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Effective</th>
            <th>From</th>
            <th>To</th>
            <th>Note</th>
            <th class="text-end">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var t in Model)
        {
            <tr>
                <td>@t.EffectiveDate</td>
                <td>@t.FromDepartment?.Name</td>
                <td>@t.ToDepartment?.Name</td>
                <td>@t.Note</td>
                <td class="text-end">
                    <a class="btn btn-sm btn-outline-secondary" asp-action="Edit" asp-route-id="@t.Id">Edit</a>
                    <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@t.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a class="btn btn-secondary" asp-controller="Employees" asp-action="Details" asp-route-id="@employee.Id">Back</a>

